server {
    listen 80;
    server_name jserrano.diasofonline.com;

    # Redirige HTTP → HTTPS (certbot lo activa automáticamente)
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    server_name jserrano.diasofonline.com;

    # SSL — certbot coloca estos paths automáticamente
    ssl_certificate     /etc/letsencrypt/live/jserrano.diasofonline.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/jserrano.diasofonline.com/privkey.pem;

    ssl_protocols       TLSv1.2 TLSv1.3;
    ssl_ciphers         HIGH:!aNULL:!MD5;

    # Subir imágenes hasta 10MB
    client_max_body_size 10M;

    # Proxy al contenedor Next.js
    location / {
        proxy_pass         http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header   Upgrade $http_upgrade;
        proxy_set_header   Connection 'upgrade';
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }

    # Cache para assets estáticos
    location /_next/static/ {
        proxy_pass http://127.0.0.1:3000;
        add_header Cache-Control "public, max-age=31536000, immutable";
    }
}
